#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main(){
    int n;
    cin >> n;
    int l = 0, r = 1e9 + 5, mid;
    while(l <= r){
        mid = (l + r) / 2;
        if(l > mid){
            
        }
    }
    
}